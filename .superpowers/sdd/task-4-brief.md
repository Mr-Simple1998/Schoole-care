# Task 4: Add 10-Row Mobile Paging

Modify only these existing pages as needed: `weapp/src/pages/student/list.vue`, `weapp/src/pages/teachers/teachers.vue`, `weapp/src/pages/income/income.vue`, `weapp/src/pages/campus/transactions.vue`, `weapp/src/pages/points/points.vue`, `weapp/src/pages/platform/platform.vue`. Keep current responsive markup and business flows. For high-volume list requests, add page state `{ page, size: 10, loading, done }`, pass `page` and `page_size: 10` to existing URLs, reset on filters, replace (do not append) current page arrays, set done when rows.length < 10, retain current rows on failure, disable controls while loading, and ignore stale responses with a request sequence. Use existing styles/components; no new dependencies. Run `npm.cmd run build:mp-weixin` if possible, write `.superpowers/sdd/task-4-report.md`, and report blockers.
