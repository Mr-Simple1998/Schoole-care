# Domain Context

## Terms

- **列表页**：展示同一业务实体集合的页面，例如学生、教师、收支和交易。
- **分页请求**：携带可选 `page` 与 `page_size` 的列表请求；响应仍是数组。
- **末页**：分页响应条数少于请求的 `page_size`；恰好满页时仍可能存在下一页。
- **旧调用**：未携带分页参数的既有接口调用，继续返回原有全量数组。
